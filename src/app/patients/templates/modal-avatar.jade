.modal(ng-controller="ModalController as modal")
  .modal-dialog
    .modal-content
      .modal-header
        button.close(type='button', data-dismiss='modal') ×
        h3.modal-title Seleccionar Avatar
      .modal-body
        div
          input.custom-file-input.avatar-upload(id="inp",type='file',base-sixty-four-input, ng-model="myfile", parser="resizeImage", onload="modal.onLoad")
        .cropArea
          img-crop(image='myImage', result-image='myCroppedImage')
        div Cropped Image:
        div
          img(ng-src='{{myCroppedImage}}')
      .modal-footer
        button.btn.btn-default Close