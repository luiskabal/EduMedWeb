.modal(ng-controller="modalController as modal")
  .modal-dialog
    .modal-content
      .modal-header
        button.close(type='button', data-dismiss='modal') ×
        h3.modal-title Heading
      .modal-body
        div
          | Select an image file: 
          input.custom-file-input(id="inp",type='file',base-sixty-four-input, ng-model="myfile", parser="resizeImage", onload="modal.onLoad")
        .cropArea
          img-crop(image='myImage', result-image='myCroppedImage')
        div Cropped Image:
        div
          img(ng-src='{{myCroppedImage}}')
      .modal-footer
        button.btn.btn-default Close